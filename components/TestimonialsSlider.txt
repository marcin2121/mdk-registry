// Testimonials Slider - Injected by MDK Wizard Remote
"use client"
import React from 'react';
import useEmblaCarousel from 'embla-carousel-react';
import { Quote, Star, ArrowLeft, ArrowRight } from 'lucide-react';

const testimonials = [
  {
    quote: "MDK cut our site production time in half. Code quality and ready-made blocks are top-notch. Highly recommend!",
    author: "Anna Kowalska",
    role: "CEO na Startupie",
    avatar: "https://images.unsplash.com/photo-1494790108377-be9c29b29330?q=80&w=150&auto=format&fit=crop",
    rating: 5
  },
  {
    quote: "Clean code, great animations and full flexibility. Adding a new section now takes minutes, not hours of developer work.",
    author: "Marek Nowak",
    role: "Fullstack Developer",
    avatar: "https://images.unsplash.com/photo-150700320443-3edc715c0a3e?q=80&w=150&auto=format&fit=crop",
    rating: 5
  },
  {
    quote: "The medical template looks amazing. Our patients praise the site clarity and ease of booking appointments.",
    author: "dr Jan Kowal",
    role: "Kardiolog, Medisense",
    avatar: "https://images.unsplash.com/photo-1612349317150-e413f6a5b16d?q=80&w=150&auto=format&fit=crop",
    rating: 4
  }
];

export default function TestimonialsSlider() {
  const [emblaRef, emblaApi] = useEmblaCarousel({ loop: true });

  const scrollPrev = React.useCallback(() => {
    if (emblaApi) emblaApi.scrollPrev();
  }, [emblaApi]);

  const scrollNext = React.useCallback(() => {
    if (emblaApi) emblaApi.scrollNext();
  }, [emblaApi]);

  return (
    <div className="py-20 bg-zinc-950 border-y border-zinc-900">
      <div className="max-w-4xl mx-auto px-4 relative">
        <div className="text-center mb-12">
           <Quote className="w-12 h-12 text-[var(--mdk-primary)] opacity-40 mx-auto mb-4" />
           <h2 className="text-2xl md:text-4xl font-black uppercase text-white">What Our Clients Say</h2>
        </div>

        <div className="overflow-hidden" ref={emblaRef}>
          <div className="flex">
            {testimonials.map((item, index) => (
              <div className="flex-[0_0_100%] min-w-0" key={index}>
                <div className="p-8 md:p-12 text-center flex flex-col items-center">
                   <div className="flex gap-1 mb-6">
                      {[...Array(item.rating)].map((_, i) => (
                         <Star key={i} className="w-5 h-5 fill-[var(--mdk-primary)] text-[var(--mdk-primary)]" />
                      ))}
                   </div>
                   <p className="text-lg md:text-2xl text-zinc-300 font-medium italic mb-8 max-w-2xl leading-relaxed">
                     "{item.quote}"
                   </p>
                   <img src={item.avatar} alt={item.author} className="w-14 h-14 rounded-full border-2 border-[var(--mdk-primary)] mb-3 object-cover" />
                   <div className="font-bold text-white text-base">{item.author}</div>
                   <div className="text-sm text-zinc-500">{item.role}</div>
                </div>
              </div>
            ))}
          </div>
        </div>

        <button onClick={scrollPrev} className="absolute left-0 top-1/2 -translate-y-1/2 p-3 bg-zinc-900 rounded-full text-white hover:bg-[var(--mdk-primary)] transition-colors border border-zinc-800 hidden md:block">
           <ArrowLeft size={20} />
        </button>
        <button onClick={scrollNext} className="absolute right-0 top-1/2 -translate-y-1/2 p-3 bg-zinc-900 rounded-full text-white hover:bg-[var(--mdk-primary)] transition-colors border border-zinc-800 hidden md:block">
           <ArrowRight size={20} />
        </button>
      </div>
    </div>
  );
}
