// Digital Commerce 2026 (E-Learning / Digital Products) - Premium Template
"use client"
import { useState } from "react";
import { motion, AnimatePresence } from "framer-motion";
import { ArrowRight, CheckCircle2, CreditCard, Play, ShoppingBag, Star, X, Zap } from "lucide-react";

export default function DigitalCommerce() {
  const [isAnnual, setIsAnnual] = useState(false);
  const [isVideoOpen, setIsVideoOpen] = useState(false);

  return (
    <div className="bg-zinc-950 text-white font-sans overflow-hidden w-full relative min-h-screen">
      {/* Background radial gradient */}
      <div className="absolute top-0 right-0 w-[500px] h-[500px] bg-indigo-500/10 rounded-full blur-[100px] pointer-events-none" />
      <div className="absolute bottom-0 left-0 w-[500px] h-[500px] bg-pink-500/10 rounded-full blur-[100px] pointer-events-none" />

      {/* Hero Section */}
      <section className="relative max-w-7xl mx-auto px-4 pt-32 pb-20 flex flex-col md:flex-row items-center gap-12">
        <motion.div 
          className="flex-1 text-center md:text-left"
          initial={{ opacity: 0, x: -30 }}
          animate={{ opacity: 1, x: 0 }}
          transition={{ duration: 0.6 }}
        >
          <div className="inline-flex items-center gap-2 bg-zinc-900 border border-zinc-800 rounded-full px-4 py-2 mb-6">
             <Star className="w-4 h-4 text-amber-500 fill-amber-500" />
             <span className="text-xs font-medium text-zinc-300">LIMITED EDITION 2026</span>
          </div>

          <h1 className="text-4xl md:text-6xl font-black tracking-tighter leading-none mb-6">
            Master the <span className="text-transparent bg-clip-text bg-gradient-to-r from-indigo-400 via-purple-500 to-pink-500">Digital Economy</span>.
          </h1>

          <p className="text-zinc-400 max-w-lg mb-8 text-base md:text-lg">
            Build, scale e-commerce, and automate your digital asset Empire with modern frameworks and AI shortcuts.
          </p>

          <div className="flex flex-col sm:flex-row gap-4 justify-center md:justify-start">
             <button className="px-8 py-4 bg-gradient-to-r from-indigo-500 to-purple-600 hover:opacity-90 font-bold uppercase tracking-wide text-sm rounded-xl transition-all shadow-lg flex items-center justify-center gap-2">
                Buy Bundle <ShoppingBag className="w-4 h-4" />
             </button>
             <button 
                onClick={() => setIsVideoOpen(true)}
                className="px-8 py-4 bg-zinc-900 border border-zinc-800 hover:bg-zinc-800/80 font-medium text-sm rounded-xl transition-all flex items-center justify-center gap-2"
             >
                Watch Teaser <Play className="w-4 h-4 text-zinc-400" />
             </button>
          </div>
        </motion.div>

        {/* Floating card mockup */}
        <motion.div 
          className="flex-1 w-full flex justify-center"
          initial={{ opacity: 0, x: 30 }}
          animate={{ opacity: 1, x: 0 }}
          transition={{ duration: 0.6, delay: 0.2 }}
        >
          <div className="relative group max-w-md w-full">
             <div className="absolute -inset-1 bg-gradient-to-r from-indigo-500 to-pink-500 rounded-2xl opacity-30 blur-md group-hover:opacity-50 transition-opacity" />
             <div className="relative bg-zinc-900 border border-zinc-800 p-6 rounded-2xl flex flex-col backdrop-blur-md">
                <div 
                   onClick={() => setIsVideoOpen(true)}
                   className="aspect-video bg-gradient-to-br from-indigo-950 to-purple-950 border border-zinc-800 rounded-xl mb-4 flex items-center justify-center overflow-hidden relative group cursor-pointer"
                >
                   <div className="absolute inset-0 bg-black/40 group-hover:bg-black/20 transition-all flex items-center justify-center">
                      <div className="w-14 h-14 bg-white/10 backdrop-blur-md rounded-full flex items-center justify-center border border-white/20 hover:scale-110 transition-transform">
                         <Play className="w-5 h-5 text-white fill-white" />
                      </div>
                   </div>
                </div>
                <h3 className="text-xl font-bold mb-2">The 2026 Creator Bundle</h3>
                <p className="text-zinc-500 text-sm mb-4">40+ hours of video, 10+ code templates, Notion dashboard.</p>
                <div className="flex items-center gap-2 text-2xl font-black text-white">
                   {isAnnual ? '$490' : '$49'} <span className="text-zinc-600 text-sm font-normal">/ {isAnnual ? 'year' : 'month'}</span>
                </div>
             </div>
          </div>
        </motion.div>
      </section>

      {/* Pricing Toggle Section */}
      <section className="max-w-md mx-auto px-4 text-center mb-10">
         <div className="inline-flex items-center bg-zinc-900 border border-zinc-800 p-1 rounded-xl">
            <button 
               className={`px-4 py-2 rounded-lg text-xs font-bold uppercase transition-all ${!isAnnual ? 'bg-indigo-500 text-black' : 'text-zinc-400'}`}
               onClick={() => setIsAnnual(false)}
            >
               Monthly
            </button>
            <button 
               className={`px-4 py-2 rounded-lg text-xs font-bold uppercase transition-all ${isAnnual ? 'bg-indigo-500 text-black' : 'text-zinc-400'}`}
               onClick={() => setIsAnnual(true)}
            >
               Annual
            </button>
         </div>
      </section>

      {/* Modules List Grid */}
      <section className="max-w-7xl mx-auto px-4 py-16 border-t border-zinc-900">
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          {['AI Video Editing', 'Scale Operations', 'React Templates', 'Marketing Ads'].map((item, i) => (
             <motion.div 
                key={i}
                className="p-6 border border-zinc-900 bg-zinc-950 rounded-xl flex items-center gap-4 hover:border-zinc-800 transition-all"
                initial={{ opacity: 0, y: 10 }}
                whileInView={{ opacity: 1, y: 0 }}
                transition={{ delay: i * 0.1 }}
                viewport={{ once: true }}
             >
                <div className="p-3 bg-zinc-900 rounded-lg text-indigo-400">
                   <Zap className="w-5 h-5" />
                </div>
                <div>
                   <h4 className="font-bold text-white text-sm">{item}</h4>
                   <p className="text-zinc-500 text-xs">Included in plan</p>
                </div>
             </motion.div>
          ))}
        </div>
      </section>

      {/* Video Modal */}
      <AnimatePresence>
        {isVideoOpen && (
          <div className="fixed inset-0 z-50 flex items-center justify-center p-4">
             <motion.div 
                className="absolute inset-0 bg-black/90 backdrop-blur-md"
                initial={{ opacity: 0 }}
                animate={{ opacity: 1 }}
                exit={{ opacity: 0 }}
                onClick={() => setIsVideoOpen(false)}
             />
             <motion.div 
                className="relative bg-zinc-900 border border-zinc-800 rounded-2xl max-w-3xl w-full aspect-video flex items-center justify-center overflow-hidden shadow-2xl"
                initial={{ opacity: 0, scale: 0.95 }}
                animate={{ opacity: 1, scale: 1 }}
                exit={{ opacity: 0, scale: 0.95 }}
             >
                <button 
                   onClick={() => setIsVideoOpen(false)}
                   className="absolute top-4 right-4 text-zinc-500 hover:text-white z-10 bg-black/40 p-1 rounded-full"
                >
                   <X className="w-5 h-5" />
                </button>

                <div className="text-center p-8">
                   <div className="w-20 h-20 bg-indigo-500/20 rounded-full flex items-center justify-center mx-auto mb-4 border border-indigo-500/40">
                      <Play className="w-8 h-8 text-indigo-400 fill-indigo-400" />
                   </div>
                   <h3 className="text-2xl font-bold mb-2">Streaming Teaser...</h3>
                   <p className="text-zinc-500 text-sm">Place your actual dashboard or cinematic promo video here.</p>
                </div>
             </motion.div>
          </div>
        )}
      </AnimatePresence>

      {/* Pricing / CTA */}
      <section className="max-w-4xl mx-auto px-4 py-20 text-center">
         <motion.div 
            className="p-12 border border-zinc-800 bg-zinc-900/40 rounded-3xl backdrop-blur-xl relative"
            initial={{ opacity: 0, scale: 0.95 }}
            whileInView={{ opacity: 1, scale: 1 }}
            viewport={{ once: true }}
         >
            <div className="absolute top-0 left-1/2 -translate-x-1/2 -translate-y-1/2 bg-gradient-to-r from-pink-500 to-indigo-500 text-black font-black uppercase text-xs px-6 py-2 rounded-full shadow-lg">
               Most Popular
            </div>
            <h2 className="text-3xl md:text-4xl font-black mb-2">Get Immediate Access</h2>
            <p className="text-zinc-500 max-w-md mx-auto mb-6 text-sm">Join over 10,000+ satisfied developers and creators building profitable businesses today.</p>
            
            <div className="flex flex-col items-center gap-4">
               <div className="text-4xl font-black mb-2">
                  {isAnnual ? '$490' : '$49'} <span className="text-zinc-600 font-normal text-sm">/ {isAnnual ? 'Lifetime' : 'Month'}</span>
               </div>
               
               <button className="w-full sm:w-80 py-4 bg-white hover:bg-zinc-200 text-black font-black uppercase tracking-wide text-sm rounded-xl flex justify-center items-center gap-2 group">
                  Secure Checkout <CreditCard className="w-4 h-4 group-hover:scale-110 transition-transform" />
               </button>
               <div className="flex items-center gap-2 text-xs text-zinc-600">
                  <CheckCircle2 className="w-4 h-4 text-emerald-500" />
                  <span>30-Day Money Back Guarantee</span>
               </div>
            </div>
         </motion.div>
      </section>
    </div>
  );
}
